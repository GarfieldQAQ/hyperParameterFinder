# 实现路径

## 目标

`hyperParameterFinder` 的目标不是从零发明 PID 自动整定算法，而是把 Xtepper 接入已有成熟调参方法。

项目边界应当很清楚：

- 我们自己实现硬件实验适配层
- 复用成熟 PID 整定思想和优化器
- 避免把时间花在重复造搜索算法轮子上

也就是说，本项目真正要解决的是：

```text
Xtepper 固件命令
串口数据采集
VOFA 数据解析
安全实验编排
实验结果评分
现成优化器/整定方法适配
```

而不是重新发明：

```text
Bayesian Optimization
PSO / GA
Relay autotune
SIMC / IMC
梯度优化
MPC
```

第一阶段聚焦 FOC 电流环 PI 参数，正式实现使用 C++17：

- 使用 CMake 构建主机端工具
- 自动连接 Windows 串口
- 自动下发固件 terminal 命令
- 自动采集 VOFA JustFloat 数据
- 自动执行 `iq` 阶跃实验
- 自动评价 `ckp/cki` 参数组合
- 输出 CSV 数据和推荐结果

后续再扩展到速度环、位置环、八步换向速度环、编码器修正表诊断。

## 方法选择原则

不要一开始就把问题当成“黑盒全局寻优”。正确顺序是：

1. 先拿到安全 baseline
2. 再在 baseline 附近小范围优化
3. 如果对象表现出强约束、强耦合、多工况，再考虑增益调度或 MPC

对 Xtepper 当前阶段，推荐优先级如下：

| 阶段 | 方法 | 原因 |
|---|---|---|
| 电流环初值 | 小信号阶跃 + 保守 PI 规则 / 手工基线 | 风险低，先保证能稳定闭环 |
| 电流环微调 | 受限范围搜索 / Bayesian Optimization | 每次上机实验有成本，BO 比大网格更省实验次数 |
| 速度环初值 | 电流环稳定后做速度阶跃 | 速度环依赖电流环，不应反过来调 |
| 速度环微调 | BO / 坐标搜索 / 多目标评分 | 可同时约束速度纹波、超调、`targetIq` 饱和 |
| 强位置相关波动 | 单独诊断编码器/机械/修正表 | 不应混进 PID 搜索里掩盖根因 |
| 多工况明显 | 增益调度 | 比单组 PID 硬覆盖更现实 |

当前 C++ 程序里的网格搜索只作为第一版 smoke test 和基线工具，不作为最终优化算法方向。

## 总体架构

推荐分成四层：

1. 串口通信层

负责打开串口、发送命令、读取原始字节流、处理超时和异常关闭。

当前对应：

```text
XtepperSerial
```

正式实现使用 Win32 串口 API，避免 Python 运行时和 `pyserial` 依赖。后续如果要支持 Linux/macOS，再增加 POSIX 串口后端。

2. 数据解析层

负责把固件输出转成结构化数据。

当前优先支持 VOFA JustFloat：

```text
JustFloatParser
```

电流环通道顺序固定为：

```text
targetId,targetIq,id,iq,idRaw,iqRaw,vd,vq,va,vb,id_i,iq_i
```

后续可以加入文本流解析，例如 `stream current`、`adc on`、`enc on`。

3. 实验编排层

负责把一次实验拆成可重复步骤：

```text
foc off
foc prep current
foc limit X
foc ckp X
foc cki X
foc id 0
foc iq 0
vofa current on
foc on
等待稳定
foc iq step_value
采集响应
foc iq 0
foc off
vofa off
```

这一层必须保证实验结束后回到安全状态，即使中途异常也要尝试关闭输出。

4. 优化器适配层

负责把硬件实验包装成统一 objective，交给成熟优化器或整定方法。

当前 C++ 内置两个策略：

```text
grid:
  ckp: start:stop:step
  cki: start:stop:step

bo:
  初始设计点
  RBF 高斯过程
  Expected Improvement
  候选点池采样
```

BO 版本只面向当前二维电流环参数，目的是降低上机试验次数，不把它扩展成通用优化框架。后续仍应做插件式适配：

```text
optimizer proposes parameters
  -> experiment runner applies parameters
  -> serial collector records response
  -> scorer returns objective metrics
  -> optimizer decides next candidate
```

优先适配：

- Relay / step tuning 作为安全 baseline
- Bayesian Optimization 用于低实验预算微调
- 坐标搜索作为可解释的保守备选
- Nevergrad / PSO / GA 只作为对照，不作为上机首选

## 当前第一阶段流程

第一阶段只做电流环 `iq` 阶跃。

输入：

```text
串口号
波特率
ckp 搜索范围
cki 搜索范围
iq 阶跃目标
FOC 输出限幅
稳定时间
采集时间
VOFA 采样周期
```

输出：

```text
output/current_YYYYMMDD_HHMMSS/
  summary.csv
  current_kp_x_ki_y.csv
  ...
```

每组参数一份响应 CSV，`summary.csv` 按评分从好到坏排序。

## 电流环评分指标

当前评分使用以下指标：

- `overshoot`：超调比例
- `settle_error`：末段平均值和目标值的相对误差
- `ripple`：末段电流纹波，使用标准差估计
- `rise_time_s`：达到目标 90% 的时间
- `saturation`：`vq` 接近输出限幅的比例
- `sample_count`：采集到的有效帧数
- `effective_ratio`：`|vq|` 超过闭环有效占空比阈值的样本比例

当前硬件约从 18% 占空比后才进入电流闭环可观测区，因此默认：

```text
closed_loop_duty = 0.18
```

评分时只使用 `|vq| >= closed_loop_duty` 后的样本评价 PI 动态。如果有效样本太少，说明实验主要落在低占空比不可观测区，此时应重罚，而不是把“小输出下看起来不动”误判为 PID 稳定。

当前综合评分：

```text
score =
  4 * overshoot
+ 5 * settle_error
+ 3 * ripple
+ 2 * saturation
+ 4 * (1 - effective_ratio)
+ normalized_rise_time
```

分数越低越好。

这个评分是保守版本：它更讨厌超调、稳态误差和纹波，而不是盲目追求最快响应。对当前硬件更合适，因为电流采样链路仍然受 PWM 窗口、DRV8701 SO 建立时间和低占空比拟合影响。

## 推荐整定策略

### 电流环

先用小范围保守实验验证数据链路：

```bash
hyperParameterFinder.exe --port COM7 --kp 0.005:0.05:0.005 --ki 0.5:5.0:0.5 --target-iq 0.2 --limit 0.3
```

这一步不是为了找到“理论最优”，而是为了确认：

- 串口收发稳定
- VOFA 帧解析正确
- 电流阶跃实验可重复
- 评分指标和实际波形一致
- 安全停止逻辑可靠

确认后，正式调参可以使用内置 BO 或外部 BO：

```text
安全 baseline
  -> 缩小参数范围
  -> 低预算 BO 或坐标搜索
  -> 重复实验确认鲁棒性
```

内置 BO 示例：

```bash
hyperParameterFinder.exe --port COM7 --optimizer bo --kp 0.005:0.05:0.005 --ki 0.5:5.0:0.5 --bo-initial 5 --bo-iterations 20 --target-iq 0.2 --limit 0.3
```

内置 BO 的实现边界：

- 只搜索 `ckp/cki`
- 参数会按给定范围归一化到 0..1
- 高斯过程使用 RBF kernel
- acquisition 使用 Expected Improvement
- 候选点通过固定数量随机采样产生
- 不处理离散/条件参数
- 不替代 Optuna、MATLAB、LabVIEW 等成熟工具

如果响应太慢：

- 逐步提高 `ckp`
- 保持 `cki` 中等
- 观察 `rise_time_s` 和 `settle_error`

如果出现明显振荡：

- 降低 `ckp`
- 降低 `cki`
- 观察 `ripple` 和 `overshoot`

如果长期有稳态误差：

- 在不过度增加纹波的前提下提高 `cki`

如果 `saturation` 很高：

- 当前参数不能只靠 PI 判断
- 需要同时看 `foc limit`、电源电压、电机负载、电流采样是否有效

### 速度环

速度环要在电流环可用之后再调。

速度环实验需要采集：

```text
targetMechanicalSpeed
mechanicalSpeed
mechanicalSpeedRaw
targetIq
iq
vq
velocityIntegral
```

评分指标类似：

- 速度超调
- 速度稳态误差
- 速度纹波
- `targetIq` 是否长期限幅
- 电流环是否能跟随 `targetIq`

### 位置环

位置环最后调。

位置环需要先确认：

- 编码器零点有效
- 400 点修正表没有明显位置相关突变
- 速度环已经稳定

位置环评分重点：

- 到位时间
- 位置超调
- 静态误差
- 到位后速度纹波
- 是否出现位置相关周期波谷

## 安全边界

自动调参必须默认保守。

建议第一版约束：

- 默认 `target_iq <= 0.2`
- 默认 `foc limit <= 0.3`
- 每组参数测试前先 `foc off`
- 每组参数测试后强制 `foc iq 0`、`foc off`、`vofa off`
- 如果串口异常或脚本中断，也要在 `finally` 里尝试关闭输出
- 后续增加温度、电源电流或 fault 状态监控后，可以做自动停止

不建议一开始就做高速速度环自动搜索。速度环会让电机持续运动，风险比电流环高，需要先完成电流环和数据链路验证。

## 和固件命令的关系

当前脚本依赖以下固件命令：

```text
foc prep current
foc limit X
foc ckp X
foc cki X
foc id X
foc iq X
vofa current on
vofa off
foc on
foc off
```

固件侧命令列表已经开始收敛，后续主机脚本应尽量只依赖正式命令，不依赖隐藏别名。

## 后续开发里程碑

### M1：电流环自动搜索可用

- 完成串口连接
- 完成 VOFA JustFloat 解析
- 完成 `iq` 阶跃实验
- 完成 CSV 输出
- 完成 `summary.csv` 排序
- 人工检查 3 到 5 组参数结果是否符合直觉

### M2：优化器解耦

- 把 C++ 程序拆成 experiment runner
- 支持 JSON/CSV 输入输出
- 让外部优化器调用一次实验并获得评分
- C++ 侧不绑定具体优化算法

建议接口：

```text
hyperParameterFinder run-current --config trial.json --result result.json
```

这样 Optuna、bayesian-optimization、Nevergrad，甚至 MATLAB/LabVIEW 都可以把它当硬件实验执行器调用。

### M3：结果可视化

- 为每组参数生成 SVG
- summary 中链接到对应 SVG
- 图中显示 `targetIq`、`iq`、`iqRaw`、`vq`
- 标注超调、稳态窗口、评分

### M4：成熟优化器接入

优先顺序：

```text
1. 坐标搜索 / 局部搜索
2. Bayesian Optimization
3. Nevergrad/PSO/GA 作为对照组
```

上机实验不优先使用 GA/PSO/RL，因为它们通常需要更多试验次数，安全成本更高。

### M5：速度环搜索

- 新增 `--loop velocity`
- 使用 `vofa velocity on`
- 自动设置 `foc vkp/vki/vmax/vel`
- 评分速度响应
- 检查 `targetIq` 是否饱和
- 优先在电流环已确认稳定后进行

### M6：实验配置文件

支持 YAML 或 JSON 配置：

```text
port
baud
loop
safe_limit
target
kp_range
ki_range
capture_time
settle_time
```

这样每次实验可复现。

### M7：固件状态守护

实验中周期检查：

- `fault`
- `foc`
- ADC sample count
- 保护锁存
- VOFA 帧率

如果异常，立即停止实验。

## 当前代码状态

当前第一版集中在：

```text
CMakeLists.txt
src/main.cpp
```

随着功能增加，建议拆分为：

```text
hyper_parameter_finder/
  serial_link.hpp/.cpp
  vofa.hpp/.cpp
  experiments.hpp/.cpp
  scoring.hpp/.cpp
  search.hpp/.cpp
  plots.hpp/.cpp
src/main.cpp
```

`src/main.cpp` 最终只保留 CLI 入口，核心逻辑移入库代码内。

长期定位应是：

```text
C++ 高效硬件实验执行器
+ 成熟外部优化器/整定方法
+ 可复现实验数据和图表
```

而不是：

```text
自研一个新的 PID optimizer
```
