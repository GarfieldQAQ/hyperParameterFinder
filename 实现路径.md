# 实现路径

## 目标

`hyperParameterFinder` 的目标是把 Xtepper 的控制参数调试从“人工输入命令、截图观察、凭经验改参数”推进到“自动实验、自动记录、自动评分、给出候选参数”。

第一阶段聚焦 FOC 电流环 PI 参数：

- 自动连接串口
- 自动下发固件 terminal 命令
- 自动采集 VOFA JustFloat 数据
- 自动执行 `iq` 阶跃实验
- 自动评价 `ckp/cki` 参数组合
- 输出 CSV 数据和推荐结果

后续再扩展到速度环、位置环、八步换向速度环、编码器修正表诊断。

## 总体架构

推荐分成四层：

1. 串口通信层

负责打开串口、发送命令、读取原始字节流、处理超时和异常关闭。

当前对应：

```text
XtepperSerial
```

2. 数据解析层

负责把固件输出转成结构化数据。

当前优先支持 VOFA JustFloat：

```text
JustFloatParser
```

电流环通道顺序固定为：

```text
targetId,targetIq,id,iq,idRaw,iqRaw,vd,vq,va,vb,id_i,iq_i
```

后续可以加入文本流解析，例如 `stream current`、`adc on`、`enc on`。

3. 实验编排层

负责把一次实验拆成可重复步骤：

```text
foc off
foc prep current
foc limit X
foc ckp X
foc cki X
foc id 0
foc iq 0
vofa current on
foc on
等待稳定
foc iq step_value
采集响应
foc iq 0
foc off
vofa off
```

这一层必须保证实验结束后回到安全状态，即使中途异常也要尝试关闭输出。

4. 参数搜索与评分层

负责生成候选参数、执行实验、计算指标、排序输出。

当前第一版使用网格搜索：

```text
ckp: start:stop:step
cki: start:stop:step
```

后续可以替换成分阶段搜索、贝叶斯优化、坐标搜索或多目标优化。

## 当前第一阶段流程

第一阶段只做电流环 `iq` 阶跃。

输入：

```text
串口号
波特率
ckp 搜索范围
cki 搜索范围
iq 阶跃目标
FOC 输出限幅
稳定时间
采集时间
VOFA 采样周期
```

输出：

```text
output/current_YYYYMMDD_HHMMSS/
  summary.csv
  current_kp_x_ki_y.csv
  ...
```

每组参数一份响应 CSV，`summary.csv` 按评分从好到坏排序。

## 电流环评分指标

当前评分使用以下指标：

- `overshoot`：超调比例
- `settle_error`：末段平均值和目标值的相对误差
- `ripple`：末段电流纹波，使用标准差估计
- `rise_time_s`：达到目标 90% 的时间
- `saturation`：`vq` 接近输出限幅的比例
- `sample_count`：采集到的有效帧数

当前综合评分：

```text
score =
  4 * overshoot
+ 5 * settle_error
+ 3 * ripple
+ 2 * saturation
+ normalized_rise_time
```

分数越低越好。

这个评分是保守版本：它更讨厌超调、稳态误差和纹波，而不是盲目追求最快响应。对当前硬件更合适，因为电流采样链路仍然受 PWM 窗口、DRV8701 SO 建立时间和低占空比拟合影响。

## 推荐搜索策略

### 电流环

先用小范围保守搜索：

```bash
python tune_foc_pi.py --port COM7 --kp 0.005:0.05:0.005 --ki 0.5:5.0:0.5 --target-iq 0.2 --limit 0.3
```

如果响应太慢：

- 逐步提高 `ckp`
- 保持 `cki` 中等
- 观察 `rise_time_s` 和 `settle_error`

如果出现明显振荡：

- 降低 `ckp`
- 降低 `cki`
- 观察 `ripple` 和 `overshoot`

如果长期有稳态误差：

- 在不过度增加纹波的前提下提高 `cki`

如果 `saturation` 很高：

- 当前参数不能只靠 PI 判断
- 需要同时看 `foc limit`、电源电压、电机负载、电流采样是否有效

### 速度环

速度环要在电流环可用之后再调。

速度环实验需要采集：

```text
targetMechanicalSpeed
mechanicalSpeed
mechanicalSpeedRaw
targetIq
iq
vq
velocityIntegral
```

评分指标类似：

- 速度超调
- 速度稳态误差
- 速度纹波
- `targetIq` 是否长期限幅
- 电流环是否能跟随 `targetIq`

### 位置环

位置环最后调。

位置环需要先确认：

- 编码器零点有效
- 400 点修正表没有明显位置相关突变
- 速度环已经稳定

位置环评分重点：

- 到位时间
- 位置超调
- 静态误差
- 到位后速度纹波
- 是否出现位置相关周期波谷

## 安全边界

自动调参必须默认保守。

建议第一版约束：

- 默认 `target_iq <= 0.2`
- 默认 `foc limit <= 0.3`
- 每组参数测试前先 `foc off`
- 每组参数测试后强制 `foc iq 0`、`foc off`、`vofa off`
- 如果串口异常或脚本中断，也要在 `finally` 里尝试关闭输出
- 后续增加温度、电源电流或 fault 状态监控后，可以做自动停止

不建议一开始就做高速速度环自动搜索。速度环会让电机持续运动，风险比电流环高，需要先完成电流环和数据链路验证。

## 和固件命令的关系

当前脚本依赖以下固件命令：

```text
foc prep current
foc limit X
foc ckp X
foc cki X
foc id X
foc iq X
vofa current on
vofa off
foc on
foc off
```

固件侧命令列表已经开始收敛，后续主机脚本应尽量只依赖正式命令，不依赖隐藏别名。

## 后续开发里程碑

### M1：电流环自动搜索可用

- 完成串口连接
- 完成 VOFA JustFloat 解析
- 完成 `iq` 阶跃实验
- 完成 CSV 输出
- 完成 `summary.csv` 排序
- 人工检查 3 到 5 组参数结果是否符合直觉

### M2：结果可视化

- 为每组参数生成 SVG
- summary 中链接到对应 SVG
- 图中显示 `targetIq`、`iq`、`iqRaw`、`vq`
- 标注超调、稳态窗口、评分

### M3：两阶段搜索

第一阶段粗扫：

```text
ckp 大步进
cki 大步进
```

第二阶段围绕最优点细扫：

```text
ckp 小步进
cki 小步进
```

避免全空间网格搜索过慢。

### M4：速度环搜索

- 新增 `--loop velocity`
- 使用 `vofa velocity on`
- 自动设置 `foc vkp/vki/vmax/vel`
- 评分速度响应
- 检查 `targetIq` 是否饱和

### M5：实验配置文件

支持 YAML 或 JSON 配置：

```text
port
baud
loop
safe_limit
target
kp_range
ki_range
capture_time
settle_time
```

这样每次实验可复现。

### M6：固件状态守护

实验中周期检查：

- `fault`
- `foc`
- ADC sample count
- 保护锁存
- VOFA 帧率

如果异常，立即停止实验。

## 当前代码状态

当前第一版集中在单文件：

```text
tune_foc_pi.py
```

随着功能增加，建议拆分为：

```text
hyper_parameter_finder/
  serial_link.py
  vofa.py
  experiments.py
  scoring.py
  search.py
  plots.py
tune_foc_pi.py
```

`tune_foc_pi.py` 最终只保留 CLI 入口，核心逻辑移入包内。
